describe('Product fuzzy search e2e scenarios', () => {
  it.todo(
    'GET /products/public/:slug returns Arabic spelling variants from the requested tenant only',
  );

  it.todo(
    'GET /products/public/:slug rejects empty or whitespace-only search text',
  );

  it.todo(
    'GET /products/public/:slug returns stable pagination metadata for normalized search',
  );
});
